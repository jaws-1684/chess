require 'chess/validator'
require 'chess/board'
require 'chess/piece'

module Chess
	describe Pawn do
		context "when white" do
			it "can step"
			it "can double step"
			it "does not move to invalid positions"
		end
		context "when black" do
			it "can step"
			it "can double step"
			it "does not move to invalid positions"
		end
	end
end
